import React from "react";

function Navbar() {
    return (
        <header>
            <nav className="navbar_container">
                <h4>{"<App's Name>"}</h4>
            </nav>
        </header>
    );
}

export default Navbar;
